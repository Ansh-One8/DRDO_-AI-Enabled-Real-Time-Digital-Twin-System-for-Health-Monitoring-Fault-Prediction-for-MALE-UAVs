# MALE UAV Aero-Piston Engine Digital Twin -- pinned runtime deps
# Python 3.11-3.14.  Install:  pip install -r requirements.txt
#
# Core numerics / ML
numpy==2.5.3
pandas==3.0.5
scipy==1.18.1
scikit-learn==1.9.0
xgboost==3.4.1
joblib==1.6.0

# Explainability
shap==0.52.0
numba==0.67.0            # shap TreeExplainer acceleration
llvmlite==0.49.0

# ECU / FADEC telemetry transport
python-can==4.6.1        # virtual bus backend; swap channel for SocketCAN in the field

# Dashboard
streamlit==1.63.0
plotly==7.0.0
pyyaml==6.0.3

# Optional: distributed / fleet deployment path (not needed for the demo)
paho-mqtt==2.1.0
amqtt==0.12.0
